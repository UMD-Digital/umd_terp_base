{#
/**
 * @file
 * Theme override to display a UMD Terp Link set Paragraph.
 */
#}

{%
  set classes = [
    'link-set',
		paragraph.field_ut_button_center.value ? 'link-set-centered',
  ]
%}

{% block paragraph %}
	<div{{attributes.addClass(classes).setAttribute('id','umd_terp_paragraph--'~paragraph.id())}}>
		{% block content %}
			{{ content.field_ut_link_set_links|field_value }}
		{% endblock %}
		{{ content|without('field_ut_link_set_links', 'field_ut_button_center') }}
	</div>
{% endblock paragraph %}
